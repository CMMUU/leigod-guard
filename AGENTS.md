# 项目维护约定

本项目的后续自动更新改动遵循用户确认的规则，完整说明见 [docs/UPDATES.md](docs/UPDATES.md)：

- 默认 `auto`（国内优先）；比较可用的正式新版本，同版本优先 Gitee，GitHub 作为备用。
- 保留“仅 Gitee”和“仅 GitHub”。明确选择单一来源时，检查、校验和下载均只连接该来源；保留已有配置中的明确选择。
- 自动模式并行、限时检查。来源失败不能报告为“已是最新版”。仅在用户点击后下载并应用更新。
- 备用下载固定同一版本、安装方式、文件名、大小及已经确认的 SHA-256；重新下载并校验，不拼接不同来源的残片，不降级，不换装其他版本。
- 每次正式发布同时提供安装 EXE、绿色 ZIP 和 `SHA256SUMS.txt`。GitHub 构建一次，Gitee 同步相同资产；分别核实发布与同步结果。
- 更新器改动需覆盖来源选择、断网、版本不同步、严格单源、错误文件和配置兼容。真实安装测试仅使用隔离测试环境，不启动或更新用户正在使用的应用。

README、隐私说明、发版说明与实际行为保持一致。没有完成的外部发布或真实账户验证不得写为成功。
