# 版本记录

版本号采用 `主版本.次版本.修订版本` 格式；Git 标签在前面加 `v`。本记录从首次公开发布开始，不追溯未经公开发布的开发版本。

## 未发布

## 0.8.3 — 2026-09-05

- 修复启用游戏加加屏蔽后，`GPP64.dll` 等注入模块被拒绝时可能出现 Windows「损坏的映像 / 0xc0000428」弹窗的问题。受保护进程在加载 DLL 之前就继承非交互式错误处理，加载失败直接返回调用方，避免弹窗阻塞启动。
- 保留严格 DLL 加载限制、其他错误处理标志和崩溃报告；不放行被拦截的模块，不关闭游戏加加，也不修改游戏进程或系统设置。
- 新增 Windows MSVC 子进程回归测试：同一份无害 DLL 在普通进程中可加载，在受保护进程中被签名策略拒绝，并验证错误处理继承和限时退出。测试不使用雷神账号或执行暂停；实际游戏加加与各显卡组合仍需用户环境复核。
- README 增加该报错的原因、升级和旧版临时关闭屏蔽的处理方法；继续提供安装版 EXE、绿色版 ZIP 与 GitHub / Gitee 更新来源。

## 0.8.2 — 2026-09-05

- 「关于与更新」新增更新来源下拉，可选择 GitHub 或 Gitee（国内）。版本检查、安装包、绿色包与校验文件均使用所选来源，选择 Gitee 后无需连接 GitHub 完成更新。
- 保留旧配置的 GitHub 来源与默认关闭的启动检查；切换来源会清除上一次检查结果，重新检查后才可更新。自动检查只提示，点击「下载并更新」才下载与安装。
- Gitee 检查按版本号选择更高的正式版本，忽略草稿与预发布，避免旧版说明被编辑后误判为新版；发布附件缺失、来源不符、大小或 SHA-256 不匹配时停止更新。
- 新增 GitHub → Gitee 自动同步工作流：配置一次 `GITEE_TOKEN` 后，主分支更新同步代码，GitHub 发版成功自动复制标签、说明、安装 EXE、绿色 ZIP 和校验文件。Gitee 复用同一份构建产物，不需要再次编译或手动逐次发版。
- Gitee 新版本先以预发布状态同步，全部附件下载复核一致后才转为正式版；重跑会核对并复用已上传文件，不覆盖冲突附件。同步失败可在 GitHub Actions 重跑，用户可切换来源。
- 0.8.1 及更早版本没有 Gitee 选项；若旧版无法连接 GitHub，先从 Gitee 手动安装或解压新版一次，再使用 Gitee 应用内更新。

## 0.8.1 — 2026-09-05

- 首页集中说明自动暂停规则：启动连续无名单游戏默认等待 3 分钟、游戏全部退出默认等待 90 秒、准备游戏延后，以及跳过检查、失败重试和关机暂停的独立开关。
- 统一 README、应用内提示和发布说明中的暂停验证方法：打开雷神官方微信小程序，登录同一账号，下拉刷新后核对计时状态。
- 说明工具成功提示来自接口返回，核对自动暂停时应先只刷新查看；暂停计时的验证不代表工具停止了客户端加速。
- 「策略 → OSD 与注入屏蔽」新增默认关闭的游戏加加屏蔽选项；从托盘完全退出雷神守护并重新打开后，通过 Windows 严格的进程 DLL 加载策略保护本工具，不关闭游戏加加，也不修改其文件、注册表、进程或游戏内设置。
- 该屏蔽策略同时会拒绝其他不属于 Microsoft、Microsoft Store 或 WHQL 信任范围的 DLL，可能影响其他 OSD、录屏或输入法插件。已在 GamePP SDK 1.2.1615.625 与 NVIDIA 显卡环境实测阻止游戏加加注入；AMD、Intel 显卡环境尚未覆盖。

## 0.8.0 — 2026-09-03

- 全新“守护计时”应用图标，统一程序 EXE、窗口、托盘、快捷方式与安装器；提供多尺寸图标资源。
- 优化首页说明，解释异常关机后重启处理工作、其他任务时，通过暂停闲置计时减少不必要的时长消耗。
- 启动检查从立即暂停改为默认连续等待 180 秒，与正常游戏退出的 90 秒宽限期分开设置；旧配置缺少新字段时同样默认 180 秒。
- 首页和托盘新增「准备游戏，延后10分钟」，仅延后本次尚未结束的启动检查，重复点击从最新点击起计时，不逐次累加。设置的启动等待更长时继续等待。
- 检测到名单游戏后结束启动检查并转入正常监控；已完成或跳过的启动检查，不因后续手动开启加速重新启动。工具无法识别雷神客户端的加速按钮，也不会自动开启加速。
- 进程检查失败重计连续等待并保留准备游戏保护；慢登录后在真实暂停请求前复核名单、游戏和延后请求。失败仍按冷却策略重新检查，不标记为已暂停。
- 保留常用游戏下拉与自定义名单、托盘直接退出、应用内更新；继续同时提供 Windows x64 安装版和绿色免安装版。
- CI 和发布增加 EXE、安装器及绿色包图标资源校验。账户、游戏与关机的实际行为仍需在用户环境核对，已经消耗的时长无法追回。

## 0.7.0 — 2026-09-03

- 新增默认开启的「启动时无游戏运行则暂停计时」策略，旧配置缺少该字段时同样默认开启；可在「策略」关闭。
- 启动时自动暂停开启、名单非空且进程名全部有效，首次成功检查未发现名单游戏时立即尝试暂停，不等待退出宽限期；有游戏运行时转入原有的游戏退出监控。
- 进程检查失败不视为没有游戏，暂停失败不标记成功；暂停失败后等待 60 秒冷却，并以新的进程检查结果判断是否重试。启动时名单为空、任一进程名无效或相关开关关闭时跳过该次启动检查。
- 新增 8 项常用游戏下拉预设和「自定义 / 手动填写」入口，继续保留从运行进程选择和修改文件名；预设不代表已经逐款验证游戏、反作弊或真实账户暂停。
- 更新器本身不请求暂停；更新完成后重新打开应用会执行启动策略，满足上述条件时可能暂停计时。已经消耗的时长无法追回。
- 继续发布 Windows x64 安装版与绿色免安装版，保留托盘直接退出行为。

## 0.6.1 — 2026-09-03

- 托盘菜单「退出」改为保存配置后直接退出，不弹确认，也不因退出操作请求暂停计时。
- 账户页「退出程序」仍保留原有的暂停与退出确认流程。
- 已进入更新准备阶段时，托盘退出仍提示更新进行中，避免中断已确认的更新。

## 0.6.0 — 2026-09-03

继续同时发布 Windows x64 安装 EXE 和绿色免安装 ZIP，增加可由用户控制的应用内更新。

- 新增「关于与更新」页面，随时可以手动「检查更新」。
- 新增「启动时自动检查更新」选项，默认关闭；启用后在启动时检查 GitHub 正式发布版本，有新版时提示。
- 点击「下载并更新」才开始下载与升级，自动检查不会自行安装新版。
- 更新前核对同一 Release 的 SHA-256 校验值；安装版使用安装 EXE，绿色免安装版使用 ZIP，保留原有发行方式与用户数据。
- 升级时关闭应用，完成后重新打开；期间监控短暂停止，不会因升级操作自动暂停雷神账户计时。
- README 分别提供安装版和绿色免安装版直达链接，并说明配置仍保存在用户目录、可选自启会写入当前用户注册表。
- 应用及安装包仍未进行 Windows 代码签名；SHA-256 完整性检查不能替代代码签名。0.5.x 用户首次升级到本版本仍需手动下载安装包或解压新版 ZIP。

## 0.5.1 — 2026-09-03

新增 Windows x64 安装版，保留便携版作为备选。自动暂停的触发条件与账户功能保持不变。

- 提供 `leigod-guard-v0.5.1-windows-x64-setup.exe`，下载后双击安装，无需手动解压或配置运行库。
- 默认安装到当前用户的 `%LOCALAPPDATA%\Programs\LeigodGuard\`，无需管理员权限；创建开始菜单快捷方式，桌面快捷方式可选。
- 检测 WebView2 Runtime，缺失时通过随包的微软安装引导程序联网补装；未将完整运行时打入离线安装包。
- MSVC 发布构建静态链接 C/C++ 运行库，减少首次运行的依赖安装步骤。
- 支持从 Windows 应用列表卸载，更新和卸载保留用户目录中的配置与账户数据。
- 安装时迁移已有的本工具自启路径，卸载时仅清理指向当前安装目录的自启项。
- 增加单实例保护，重复打开时尝试唤起已有窗口；安装和卸载要求先退出正在运行的当前版本。
- 发布工作流同时生成安装 EXE、便携 ZIP 和包含两者的 SHA-256 校验文件，README 改为优先说明安装版。
- 安装程序和应用尚未进行 Windows 代码签名，暂不提供应用内自动更新。

## 0.5.0 — 2026-09-03

首次公开发布，面向 Windows 10 / 11 x64。

### 提供的功能

- 按游戏进程文件名观察运行状态；检测到名单游戏运行并全部退出后，等待宽限期，再请求暂停雷神账户计时。
- 默认 3 秒检测间隔、90 秒宽限期；支持配置与手动立即暂停。
- 密码、短信验证码及已有 token 登录；需要时显示人机验证窗口。
- 使用 Windows DPAPI 保存 token，以及用户选择保存的密码哈希凭据。
- 系统托盘运行、当前用户登录自启、关机或注销时尝试暂停。
- 账户状态与运行日志页面。

### 开源发布

- 整理项目 README、隐私说明、无个人数据的配置示例和版本记录。
- 提供 Windows x64 发布包与下载校验文件，并配置 MSVC 自动构建发布流程。
- 自有代码采用 MIT 许可证，并单独列出第三方依赖与接口研究来源。

### 发布前修正

- 进程检测失败时暂缓自动暂停，不再将扫描失败误判为游戏退出；检测恢复后重新计算连续空闲的宽限期。
- 关闭自动暂停开关会清除旧倒计时；暂停失败后的冷却期仍观察游戏重新启动，避免沿用失效的退出状态。
- 修正倒计时边界处理；手动暂停成功后清除旧状态，关机时直接尝试幂等暂停以减少网络请求。
- 避免在 API 网络错误、响应解析错误及验证码诊断日志中写入完整请求 URL、原始响应或验证凭据。
- 遮蔽手动 token 输入，移除原始账户信息展示；切换到短信或 token 登录时清除旧密码哈希，配置解析失败不回显配置内容。
- 本地诊断日志达到约 1 MiB 时轮转，保留一份备份；移除开发用调试启动入口。
- 已登录时退出会提供暂停选项，并等待本次暂停结果，避免将历史状态误判为本次成功。

### 当前限制

- 仅提供 Windows x64 版本，不提供 macOS、Linux 或手机版。
- 不自动开启加速、不自动恢复计时、不自动选择线路。
- 初始空闲不触发自动暂停；需要先观察到名单游戏运行。
- 依赖有效登录、网络、雷神服务及非公开接口，可能因接口调整而失效。
- 关机暂停是尽力而为，不能保证在断电、断网、强制退出或系统崩溃时完成。

完整行为与使用边界见 [README](README.md)。
